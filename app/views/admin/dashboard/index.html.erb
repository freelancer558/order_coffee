<div class="well">
	<h4>All [<%= @users.size %>] users</h4>
	<div>
		<ul class="nav nav-tabs nav-stacked">
			<% @users.each do |user| %>
			<li><%= user.username %></li>
			<% end %>
		</ul>
	</div>
</div>