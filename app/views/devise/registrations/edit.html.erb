
<h2>Edit <%= resource_name.to_s.humanize %></h2>
<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, :class => 'form-vertical', :multipart=> true}) do |f| %>
<%= devise_error_messages! %>
<%= f.label :username %>
<%= f.text_field :username, :autofocus => true %>
<%= f.label :email %>
<%= f.email_field :email, :required => true %>
<%= f.label :avatar %>
<%= f.file_field :photo, :required => true %>
<%= f.label :password %>
<%= f.password_field :password, :autocomplete => "off", :hint => "leave it blank if you don't want to change it", :required => false %>
<%= f.label :password_confirmation %>
<%= f.password_field :password_confirmation, :required => false %>
<%= f.label :current_password %>
<%= f.password_field :current_password, :hint => "we need your current password to confirm your changes", :required => true %>
<br />
<%= f.submit 'Update', :class => 'btn btn-primary' %>
<%= link_to 'Cancel', :back, :class => 'btn' %>
<% end %>
<h3>Cancel my account</h3>
<p><%= link_to "Cancel my account", registration_path(resource_name), :class => 'btn', :data => { :confirm => "Are you sure?" }, :method => :delete %></p>
